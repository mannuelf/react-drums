import React, { Component } from "react";
import _ from "lodash";
import { getDrums } from "../data/drums";
import styled from "styled-components";

const Button = styled.button`
  background: #444;
  border-radius: 3px;
  color: yellow;
  margin: 0 0.5em;
  padding: 1rem;
  border: none;
  height: 100%;
  width: 100%;
  cursor: pointer;
  box-shadow: -2px 2px 2px rgba(0, 0, 0, 0.4);
`;

class Pad extends Component {
  state = {
    kits: []
  };

  componentDidMount() {
    this.setState({ kits: getDrums() });
  }

  render() {
    const { length: count } = this.state.kits
    return "button";
  }
}

export default Pad;
